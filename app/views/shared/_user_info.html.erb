<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<p><%= link_to "view profile", current_user %></p>
<p><%= pluralize(current_user.microposts.count, "micropost") %></p>