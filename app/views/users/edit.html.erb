<% provide(:title, "Update your profile") %>
<h1>Update your profile</h1>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for(@user) do |f| %>
		<%= render 'fields', f: f %>
		<% end %>
		<p class="gravatar-edit"><%= gravatar_for @user %> <a href="http://gravatar.com/emails">change</a></p>
	</div>
</div>